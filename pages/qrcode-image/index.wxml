<!--pages/qrcode-image/index.wxml-->
<view class="container">
<view class="title">
  <view wx:if="{{wechatId}}">Wechat ID: {{wechatId}}</view>
  <view wx:else>wechatId: Not Yet Set</view>
<text>Long tap on the qr-code to save it to your phone， 
then scan it via wechat qr-code scanner to add the owner as your friend!</text>
</view>
   <image src="{{qrcodeUrl}}" class="item" width="355" bindlongpress="imgLongTap" height="150"></image>
   
</view>

